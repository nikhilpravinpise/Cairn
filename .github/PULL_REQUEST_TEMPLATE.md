## Description

Brief description of the change and the motivation behind it.

## Checklist

- [ ] `flutter analyze` passes
- [ ] `flutter test` passes (all 657+ tests)
- [ ] `cd scripts && pytest -q` passes
- [ ] No raw bench logs or model files committed
- [ ] Locked artifacts (`system_prompt_v1.txt`, schema) not changed without migration note
- [ ] `CHANGELOG.md` updated if user-facing
